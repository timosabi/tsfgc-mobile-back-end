info:
  name: Get my matchweek prediction slip
  type: http
  seq: 1700000025000
http:
  method: GET
  url: '{{baseUrl}}/friends-groups/{{friendsGroupId}}/matchweeks/{{matchweek}}/predictions/mine'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: 'FE placement: prediction slip screen load. Purpose: returns matchweek fixtures, current user saved score predictions, selected red-card fixture, submitted flag, and locked flag.'
