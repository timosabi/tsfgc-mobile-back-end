info:
  name: Health check
  type: http
  seq: 1700000003000
http:
  method: GET
  url: '{{baseUrl}}/health'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: 'FE placement: optional developer diagnostics or app boot health banner. Purpose: confirms the backend process is reachable before testing auth/app flows. Notes: no auth required.'
