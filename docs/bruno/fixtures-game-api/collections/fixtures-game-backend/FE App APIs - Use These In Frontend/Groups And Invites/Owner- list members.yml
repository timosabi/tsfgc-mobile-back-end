info:
  name: 'Owner: list members'
  type: http
  seq: 1700000020500
http:
  method: GET
  url: '{{baseUrl}}/friends-groups/{{friendsGroupId}}/members'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: 'FE placement: owner member-management screen. Purpose: returns the group members with profile previews and action flags for remove/transfer ownership controls. Requires current user to be group owner. Use matchweek overview for dashboard member cards; use this endpoint for settings/admin actions.'
