info:
  name: Preview invite link
  type: http
  seq: 1700000014000
http:
  method: GET
  url: '{{baseUrl}}/friends-groups/invite/{{inviteToken}}'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: 'FE placement: invite landing page opened from shared link. Purpose: validates inviteToken and displays group name, open/private access type, and member count before user joins.'
