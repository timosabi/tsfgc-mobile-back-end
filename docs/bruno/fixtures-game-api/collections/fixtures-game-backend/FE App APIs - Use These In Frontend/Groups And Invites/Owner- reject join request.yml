info:
  name: 'Owner: reject join request'
  type: http
  seq: 1700000019000
http:
  method: POST
  url: '{{baseUrl}}/friends-groups/requests/{{joinRequestId}}/reject'
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: '{}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: 'FE placement: owner reject button. Purpose: rejects a private join request without adding membership.'
