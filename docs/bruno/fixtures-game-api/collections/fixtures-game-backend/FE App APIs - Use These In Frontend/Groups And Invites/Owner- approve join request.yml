info:
  name: 'Owner: approve join request'
  type: http
  seq: 1700000018000
http:
  method: POST
  url: '{{baseUrl}}/friends-groups/requests/{{joinRequestId}}/approve'
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: '{}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: 'FE placement: owner approve button. Purpose: approves a private join request and adds the user as group member.'
