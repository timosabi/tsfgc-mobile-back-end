info:
  name: 'Owner: list pending join requests'
  type: http
  seq: 1700000017000
http:
  method: GET
  url: '{{baseUrl}}/friends-groups/{{friendsGroupId}}/requests/pending'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
docs: 'FE placement: owner pending-requests badge/list. Purpose: focused queue of pending private join requests.'
